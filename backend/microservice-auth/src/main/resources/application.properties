spring.application.name=microservice-auth
server.port=8081

# MongoDB
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://auth-mongo:27017/authdb}
spring.data.mongodb.database=authdb

# JWT — deve ser igual ao secret do api-gateway
jwt.secret=bantads-secret-key-for-jwt-signing-2025-ufpr-tads-dac-ds152
jwt.expiration-ms=86400000

# Desabilita auto-configuração relacional
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
